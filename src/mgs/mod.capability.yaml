schema_version: 1
capability_id: schema.mgs
capability_version: v1
domain: schema
name: "MGS schemas — evidence + transitions"
maturity: production
surface: repo_public
owner:
  kind: crate
  name: vertrule-schemas
implementation:
  primary_module: vertrule-schemas/src/mgs/mod.rs
  owner_files:
    - vertrule-schemas/src/mgs/evidence.rs
    - vertrule-schemas/src/mgs/transition.rs
  entrypoints:
    - type: struct
      symbol: MgsEvidence
    - type: struct
      symbol: MgsTransition
invariants:
  - id: mgs_schema_canonical
    description: MGS records serialize via JCS for cross-runtime stability
supersedes: []
