schema_version: 1
capability_id: schema.mri
capability_version: v1
domain: schema
name: "MRI schemas — batches, gradients, reductions"
maturity: production
surface: repo_public
owner:
  kind: crate
  name: vertrule-schemas
implementation:
  primary_module: vertrule-schemas/src/mri/mod.rs
  owner_files:
    - vertrule-schemas/src/mri/batch_payload.rs
    - vertrule-schemas/src/mri/gradient_coupling.rs
    - vertrule-schemas/src/mri/reduction.rs
  entrypoints:
    - type: struct
      symbol: BatchPayload
    - type: struct
      symbol: GradientCoupling
    - type: struct
      symbol: Reduction
invariants:
  - id: mri_schema_canonical
    description: All MRI payloads serialize via JCS; canonical bytes feed instrument receipts
supersedes: []
