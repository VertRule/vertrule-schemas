# Determinism policy scope for vertrule-schemas
# Layer 0: Pure schema types — no computation, no I/O

[scope]
# All source files are strictly governed
strict = ["src/**/*.rs"]

# No allowlist exceptions — vertrule-schemas has no nondeterministic operations
[allowlist]
